interface Props {
  lastDate: number;
  firstDay: number;
}

function CalenderBody({ lastDate, firstDay }: Props) {
  let dateArry = [];
  const makeCalender = () => {};

  return <>1 2 3</>;
}
export default CalenderBody;
