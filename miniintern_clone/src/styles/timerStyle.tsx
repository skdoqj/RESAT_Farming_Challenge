import { styled } from "styled-components";

export const Timer = styled.div`
  .timer {
    margin-top: 40px;
  }
  .number_area {
    font-size: 40px;
    input {
      border: 1px solid gray;
      border-radius: 5px;
      height: 60px;
      width: 60px;
      font-size: 40px;
      margin-left: 10px;
    }
  }
  .btn_area {
    position: relative;
    top: 40px;
  }
`;
